server:
  port: 8081

spring:

  h2:
    console:
      enabled: true
  
  datasource:
    url: jdbc:h2:mem:testdb
  
management:
  endpoints:
    web:
      exposure:
        include:
        - beans
        - health
        - metrics
        - env
